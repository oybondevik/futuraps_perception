# Request: XZ grid on base_link’s XZ plane (y=0), searching within lateral limits
float32 cell_size            # square size in meters
uint32  rows                 # along +Z (up), i=0 is bottom row
uint32  cols                 # along +X (forward), j=0 at x0
float32 x0                   # grid origin (forward), default 0.0
float32 z0                   # grid origin (height),  default 0.2
float32 y_left_max           # meters to the left to consider (>=0)
float32 y_right_max          # meters to the right to consider (>=0)
int8    side                 #  0: both, 1: left-only (y>=0), -1: right-only (y<=0)
float32 front_percentile     # 0..0.5 (e.g., 0.10)
uint32  min_points_per_cell  # e.g., 20
---
# Response (row-major: index = i*cols + j)
bool[]   found
float32[] x
float32[] y
float32[] z
float32[] dist
uint32[]  count
float32[] confidence
